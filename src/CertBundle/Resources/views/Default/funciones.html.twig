<script>
    /*$( function() {
        $("#descarga").click(function() {
            
        });
    });*/
    function acceso(){
        var recurso = Routing.generate("login");
        window.location = recurso;
    }
    
    
    function descargaCertificado(username) {
        alert ('usuario :'+username);
        /* var recurso = Routing.generate("descargaCertificado",{"usuario_id":usuario_id},true);
        $.ajax(recurso)
            .done(function(data) {
                AL
            })
            .fail(function(data) {
                alert ('falla en la llamada');
            });
        */
    }
</script>
